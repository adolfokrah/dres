# Test environment configuration
# This database is used for integration tests - NOT production data

# Local MongoDB test database
MONGO_PUBLIC_URL=mongodb://127.0.0.1/dres-test

# JWT secret for tests
PAYLOAD_SECRET=test-secret-for-integration-tests

# Server URL for tests
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Cron secret for tests
CRON_SECRET=test-cron-secret

# Preview secret for tests
PREVIEW_SECRET=test-preview-secret
